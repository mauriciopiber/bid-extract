# Anthropic API key for vision/extraction agents
ANTHROPIC_API_KEY=sk-ant-...

# Input directory containing bid tabulation PDFs
INPUT_DIR=/path/to/pdfs

# Output directory for extracted JSON
OUTPUT_DIR=/path/to/output
